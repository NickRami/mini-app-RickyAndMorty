{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],

  "compilerOptions": {
    "baseUrl": ".", // This is important for resolving relative paths from the root
    "paths": {
      "@/*": ["./src/*"] // Adjust './src/*' if your 'lib' folder is elsewhere, e.g., './lib/*'
    }
  }
}
